from app.programming_language_algorithms import abstract_syntax_tree as ast
from app.programming_language_algorithms import context_free_grammar as cfg

ALGOS = {
    "abstract syntax tree": ast.get_similarity,
    "context free grammar": cfg.get_similarity,
}